# Simple Interest Calculator

This repository contains a simple shell script `simple-interest.sh` that calculates the simple interest based on user-provided Principal, Rate, and Time.

## Usage
Run the script using:


bash simple-interest.sh

## Formula
Simple Interest = (Principal × Rate × Time) / 100
